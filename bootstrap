#!/usr/bin/env zsh
#
# # Setup
#
# Putting this here to just to help me set up new hardware. This guide could
# get outdated pretty quick though.
#
# ```sh
# $ git clone https://github.com/vinnicc/dotfiles.git ~
# ```
#
# Get Homebrew:
#
# ```sh
# $ /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
# ```
#
# Run:
#
# ```sh
# $ update && bootstrap
# ```
#
# Set up Vim:
#
# ```sh
# $ curl https://raw.githubusercontent.com/Shougo/neobundle.vim/master/bin/install.sh | sh && \
# vim +NeoBundleInstall +qall
# ```
#
# * Review `~/.aliases` for `fish`
# * Review `~/.skhdrc` for `yabai`
# * Use `ranger` instead of Finder
# * Change screenshot directory
#
#   ```sh
#   defaults write com.apple.screencapture location ~/Downloads
#   ```
#
# ## iTerm2
#
# * General > Window > Native full screen windows > Uncheck
# * General > Load preference from a custom folder or URL > Set path to Google Drive
# * General > Save changes to folder when iTerm2 quits > Check
# * Create hotkey window and activate with ⌥ + `
#
# ## Karabiner-Elements
#
# * Enable Simultaneous Vi Mode
# * Change Caps Lock to Keycode 53 (Escape)
# * Change Fn Key - Fn to Control_L
# * Change Control_L Key - Control_L to Fn
# * Set Delay Until Repeat - 200ms
# * Set Key Repeat - 23ms
#
# ## MacOS Tweaks
#
# Pick and choose https://github.com/mathiasbynens/dotfiles/blob/master/.macos

packages=(
  asdf
  coreutils
  emacs
  ffmpeg
  fish
  freetype
  fzf
  git
  git-extras
  gnupg
  gnutls
  imagemagick
  joplin
  lastpass-cli
  lazygit
  libffi
  libyaml
  lynx
  neovim
  openssl
  pkg-config
  ranger
  readline
  reattach-to-user-namespace
  rename
  ripgrep
  shellcheck
  ssh-copy-id
  tmux
  ucspi-tcp # `tcpserver` et al.
  wget
  yarn
  youtube-dl

  heroku/brew/heroku
  koekeishiya/formulae/yabai
  koekeishiya/formulae/skhd
  Rigellute/tap/spotify-tui
)

cask_packages=(
  # steam
  # showyedge
  adobe-creative-cloud
  caprine
  firefox
  google-backup-and-sycn
  google-chrome
  iina
  imagealpha
  imageoptim
  iterm2
  karabiner-elements
  licecap
  macs-fan-control
  postbird
  skype
  slack
  spotify
  the-unarchiver
  transmit

  homebrew/cask-fonts/font-inconsolata
  homebrew/cask-fonts/font-liberation-sans
  homebrew/cask-fonts/font-open-sans
  homebrew/cask-fonts/font-source-code-pro
  homebrew/cask-fonts/font-source-sans-pro
)

function log() {
  echo $* > /dev/stderr
}

###########################################################################

log "brew update"
brew update

log "brew upgrade"
brew upgrade

log "Installing..."
for name_and_args in $packages
do
  log "brew install ${=name_and_args}"
  brew install "${=name_and_args}"
done

for name_and_args in $cask_packages
do
  log "brew cask install ${=name_and_args}"
  brew cask install "${=name_and_args}"
done

log "Adding asdf plugins..."
asdf plugin-add ruby
asdf plugin-add erlang
asdf plugin-add elixir
asdf plugin-add postgres
asdf plugin-add redis

log "brew cleanup"
brew cleanup
